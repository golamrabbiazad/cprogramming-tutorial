#include <stdio.h>

int main() {
    /* code */
    int number[7] = {10, 15, 20, 0, 1, 2, 5};
    for (int i; i < 7; i++) {
        printf("number on index %i is equal to %i \n", number);
    }
    return 0;
}
